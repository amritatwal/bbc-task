/**
 * Template (injecting the JS into the DOM using innerHTML)
 * Test cases:
 * 
 * Basic test:
 *  1. It should render the HTML template with the given payload
 * 
 * leftTab() - on:
 *  1. It should update the time set (for p.bbc-news__time) correctly for a specific time
 *  2. It should update the time set (for p.bbc-news__time) correctly over a period of time (e.g. 12:01, 12:02...)
*/